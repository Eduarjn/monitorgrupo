# Modo dev (npm run dev): o Vite NAO le o .env.production, entao sem este arquivo
# a API cai no padrao http://localhost:3020 — que nao roda na maquina do Eduardo
# e faz o login falhar com "Failed to fetch". A API real vive no servidor.
VITE_API_URL=https://erareason.sobreip.com.br/wa-api
